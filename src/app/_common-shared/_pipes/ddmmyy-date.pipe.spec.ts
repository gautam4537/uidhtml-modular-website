import { DdmmyyDatePipe } from './ddmmyy-date.pipe';

describe('DdmmyyDatePipe', () => {
  it('create an instance', () => {
    const pipe = new DdmmyyDatePipe();
    expect(pipe).toBeTruthy();
  });
});
